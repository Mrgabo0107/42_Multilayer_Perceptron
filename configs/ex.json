{
  "model_name": "config  12",
  "topology": {
    "input_layer_activation": "tanh",
    "hidden_layers": [
      { "n_neurons": 32, "activation": "relu", "initializer": "heUniform" },
      { "n_neurons": 24, "activation": "tanh", "initializer": "xavier" },
      { "n_neurons": 16, "activation": "sigmoid", "initializer": "heNormal" },
      { "n_neurons": 260, "activation": "tanh", "initializer": "random" }
    ],
    "output_layer_initializer": "xavier"
  },
  "training": {
    "epochs": 150,
    "batch_size": 16,
    "learning_rate": 0.01,
    "loss": "categorical_crossentropy",
    "optimizer": {
      "type": "adam",
      "params": { "beta1": 0.9, "beta2": 0.999 }
    }
  },
  "early_stopping": {
    "enabled": true,
    "patience": 10,
    "monitor": "val_loss"
  }
}